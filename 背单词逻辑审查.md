# 「背单词」复习逻辑审查报告

审查范围：`app.js`（3495 行）、`cloud-sync.js`、`sw.js`、`index.html`
审查重点：复习调度（SRS）、评分与状态、每日队列与「忘了」重排、词库内联复习、云同步合并
审查时间：2026-09-10

---

## 一、结论概览

整体架构清晰、职责分层合理（数据 / 调度 / 渲染 / 同步分离），核心的「每日队列 + 艾宾浩斯式间隔 + 考试冲刺封顶」思路是自洽的。
但发现 **1 个数据级严重缺陷**、**3 个逻辑一致性问题**和若干中等/轻微问题。其中两个已用脚本复现验证。

| 级别 | 问题 | 位置 | 复审状态（v93） |
| --- | --- | --- | --- |
| P0 | `makeId` 对纯中文词返回空串，导致中文词互相覆盖 | app.js:1629 | **已修复**（v94） |
| P1 | 「重复忘了」的第二/第三次重排是死代码，永不触发 | app.js:1926 / 2239 / 1027 | **已实现**（v95，改为一次性排满 3 次复现） |
| P1 | 词库内联复习绕过「当天重复复习不记录」规则 | app.js:2533 | 行为已写进文档，仍属两套规则 |
| P1 | 复习队列中「忘了」已掌握词不降级（level 5→4 仍算掌握） | app.js:2268 | **已修复**（commit 95814a4） |
| P2 | 「错词」筛选口径与状态标签口径不一致，且错词永不消除 | app.js:2420 vs 2627 | 已在文档中明确为既定语义 |
| P2 | 记忆分布「掌握 / 完全掌握」重复计数，占比之和会 >100% | app.js:2635 | **已修复**（v94） |
| P2 | 「模糊」被计为正确，拉高「今日正确率」 | app.js:2187 | 已在文档中明确为既定语义 |
| P3 | `buildQueue` 各队列上限不一致（有的无上限，有的截 40） | app.js:1711 | 已在文档中明确为既定语义 |
| P3 | 「今日待复习」把复现副本也计入数量 | app.js:2307 | **已修复**（v95，改为去重计数） |
| P3 | `applyExamSprintSchedule` 在 `ensureBooks` 中全量扫描全部进度 | app.js:1552 | 未处理 |

> 复审说明：v93（commit `95814a4`）修复了「忘了不降级」一项；commit `14b28aa` 新增 `项目说明.md`，把多项行为（重复卡片免记录、正确率口径、队列 40 上限、错词口径）明确为**有意设计**，这些不再计为缺陷，只需保证文档与代码长期一致。
> v94 修复了 **P0 `makeId`** 与 **P2 记忆分布重复计数**；v95 实现了「忘了的词当天复现 3 次」（把原本无法触发的 P1 阶梯改为一次性排满），并修复了「今日待复习」的去重计数。
> 至此报告中所有数据级 / 一致性级问题均已闭合，仅余已声明的既定语义与少量 P3 体验项。

---

## 二、严重问题（建议优先修复）

### P0 · `makeId` 对纯中文词生成空 id → 中文词互相覆盖 —— ✅ 已修复（v94）

**原问题**：`makeId` 只保留 `[a-z0-9]`，其余字符全部替换为 `-`，纯中文词条被清空：

```js
function makeId(term) {
  return term.toLowerCase().trim().replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "");
}
```

实测："苹果" / "香蕉" / "考研 核心" 都得到 `""`，`"第1章 单词"` 只剩 `"1"`。由于 `addWord()` 用这个 id 判重，依次添加「苹果/香蕉/西瓜」后词库里只剩 **1 条** `{"id":"","term":"苹果","meaning":"西瓜"}`；且空 id 会被 `normalizeCloudWord()` 丢弃，无法同步。

**修复内容（v94）**：改为 Unicode 感知，并加短哈希兜底，保证 id 永远唯一且非空：

```js
function makeId(term) {
  const text = String(term || "");
  const readable = text
    .toLowerCase()
    .trim()
    .replace(/\s+/g, "-")
    .replace(/[^\p{L}\p{N}-]+/gu, "-")
    .replace(/-+/g, "-")
    .replace(/(^-|-$)/g, "");
  if (readable && readable.length <= 120) return readable;
  // 纯符号词条或超长词条：退化为稳定的短哈希，保证 id 唯一且非空
  const key = text.trim().toLowerCase();
  let hash = 2166136261;
  for (let index = 0; index < key.length; index += 1) {
    hash ^= key.charCodeAt(index);
    hash = Math.imul(hash, 16777619);
  }
  return `w-${(hash >>> 0).toString(36)}`;
}
```

**复审结论（v94 实测）**：

```
"苹果" => "苹果"      "香蕉" => "香蕉"      "西瓜" => "西瓜" (三者互不相同)
"泰勒 展开" => "泰勒-展开"   "第1章 单词" => "第1章-单词"   "∫x dx" => "x-dx"
"+++" => "w-u17nzc"（哈希兜底）   "Apple" => "apple"   "retail price" => "retail-price"
```

- 中文 / 中英混合词条现在生成可读且唯一的 id，添加、复习、同步全部恢复正常；
- **内置英文词条零漂移**：对 `STARTER + CORE + CET4 + CET6` 共 9312 条词条做了逐条比对，`旧 id === 新 id`，**没有一条变化**，因此不会导致任何已有进度因 id 改变而失联；
- 纯符号词条（如 `+++`）与超长词条走哈希兜底，不再产生空 id。


---

### P1 · 「重复忘了」的第二/三次重排永不触发（死代码） —— ✅ 已实现（v95）

**原问题**：常量与文案暗示存在一个多级重排阶梯，但 `rateCurrent()` 用 `isRepeatHardReview` 把复现卡片的一切点击都直接 `advanceToNext()`，导致 `hardReviewCounts` 永远停在 1，`markTodaySessionPendingHard()` 再不会被调用：

```js
const HARD_RETRY_GAPS = [4, 10];                       // 阶梯写着 4 张 / 10 张
const MAX_DAILY_HARD_REVIEWS = HARD_RETRY_GAPS.length + 1;  // = 3
if (isRepeatHardReview(word)) { advanceToNext(); return; }   // 任何评分都跳过、不记录
```

**已用脚本复现**：对同一个词反复点「忘了」，全程只产生 1 次升级，`hardReviewCounts` 永远到不了 2 / 3。

**修复内容（v95，按产品需求「忘了的词当天复现 3 次」实现）**：
把「按需逐级追加」改成「第一次忘了就一次性排满」，因此 `rateCurrent` 的拦截逻辑反而**不需要改**（复现卡片本来就只翻页）：

```js
const HARD_RETRY_GAPS = [4, 10, 20];
const HARD_RETRY_OFFSETS = [4, 14, 34];   // 累计偏移
const MAX_DAILY_HARD_REVIEWS = 4;         // 当天最多出现次数 = 1 首见 + 3 复现
```

`markTodaySessionPendingHard()` 现在把 3 个复现位置按目标下标升序一次性插入 `queueIds`，并用 `hardReviewCounts[wordId] = inserted + 1` 作为「当天出现次数」的记录。

**复审结论（v95 实测）**：
- limit=50、只忘第 1 个词 → 该词位置 `[0, 5, 15, 35]`，出现 **4 次**，返回「已安排当天再复现 3 次，约 4、14、34 张后各出现一次」；
- 极端情况 limit=200 且全部忘光 → 队列 **800 张**，恰好等于上限 `200×4`，**无溢出、每次复现都排得下**，每个词都出现 4 次；
- 复现卡片仍不写历史、不改进度（`isRepeatHardReview` 分支保持原样），当天的记录只来自第一次「忘了」。
- 实现过程中用模拟脚本抓到一个插入顺序 bug（降序插入会让后续位置漂移成 `[0,5,16,37]`），已改为升序插入并复测通过。

---

### P1 · 词库内联复习是一套独立评分路径，规则与主队列不一致

`rateInlineReview()` 直接调用 `recordRating()`，**没有**走主队列的任何保护：

```js
function rateInlineReview(rating) {
  ...
  recordRating(word, rating, { render: false });   // 直接落库
  if (rating === "hard") { inlineReviewPendingHard = true; ... }
}
```

对比主队列 `rateCurrent()`，内联路径缺失：

1. **没有 `isRepeatHardReview` 拦截** → 同一天在词库里对同一个词可以反复点「忘了」，每次都写 history、每次 `wrong +1`、每次都推进 progress。这直接违背主队列「当天重复复习不记录」的设定，两处行为不一致。
2. **没有 `demoteToUnmastered`** → 在词库里点「忘了」，已掌握词不会降级。
3. **没有 `hardReviewCounts` / 重排逻辑** → 词库复习不会把词排进当天的重点复习。

**建议**：抽出统一的 `applyRating(word, rating, context)`，让主队列与内联复习共用同一套约束（可用 `context = "due" | "library"` 控制是否允许重排/降级），避免两处逻辑漂移。

---

### P1 · 复习队列里「忘了」已掌握词，等级仍停留在「已掌握」 —— ✅ 已修复（v93）

**原问题**：`demoteToUnmastered` 只在「已掌握」队列生效，日常复习队列里 `level = 5` 的词点「忘了」只降到 4，而掌握线是 `>=4`，因此仍显示「已掌握」。

**修复内容**（commit `95814a4`）：`scheduleNext` 对「忘了」统一封顶，不再依赖队列类型：

```js
const level =
  rating === "hard"
    ? Math.max(0, Math.min(3, currentLevel - 1))   // 忘了后一定 <= 3，即脱离已掌握
    : ...
```

**复审结论**：修复正确且完整。
- `level 4 → 3`、`level 5 → 3`，两者都落到「未掌握」区间（`level < 4`），符合预期；
- `level 0/1` 时 `Math.max(0, ...)` 仍保证不为负；
- 原先的 `options.demoteToUnmastered` 分支已从 `scheduleNext` 移除，`rateCurrent` 中保留的同名变量现在只用于反馈文案（`已降为未掌握，`），语义正确。

**遗留小项（非缺陷）**：`scheduleNext(progress, rating, options = {})` 的 `options` 形参现已无人使用，`recordRating` 仍在透传 `demoteToUnmastered`。可顺手删除以保持整洁，但不影响行为。

---

## 三、中等问题

### P2 · 「错词」口径不一致，且错词永不消除

`wrong` 计数只在 `recordRating` 里 `+1`，**全局没有任何重置**（已全文件检索确认）。这导致两个口径打架：

| 位置 | 判定 | 行为 |
| --- | --- | --- |
| 词库筛选 `filter === "wrong"`（app.js:2420） | `progress.wrong > 0` | 曾经错过就一直留在「错词」列表 |
| 队列 `type === "wrong"`（app.js:1705） | `item.wrong > 0` | 同上，已掌握词也会混进「错词优先」 |
| 状态标签 `getStatus`（app.js:2627） | `wrong > 0 && level < 4` | 已掌握后显示「已掌握」 |

同一个词，标签显示「已掌握」，筛选却仍归入「错词」，自相矛盾。

**建议**：统一为「近期错词」语义——例如判定加上 `level < 4` 或在 `level` 达到 4 时把 `wrong` 清零（再或者记录 `lastWrongAt`，只统计近 N 天的错词）。

### P2 · 记忆分布重复计数 —— ✅ 已修复（v94）

**原问题**：`renderProgress` 的桶是并列统计的，「完全掌握」是「掌握」的子集：

```js
["掌握",   (p) => p.level >= 4],
["完全掌握", (p) => p.level >= 5]   // 完全掌握 ⊂ 掌握
```

`level >= 5` 的词会被同时计入两个桶，各桶之和大于词库总数，「掌握」占比被高估。

**修复内容（v94）**：改为互斥区间：

```js
["掌握",   (p) => p.level >= 4 && p.level < 5],
["完全掌握", (p) => p.level >= 5]
```

**复审结论**：五个区间（未学 / 初识 / 熟悉 / 掌握 / 完全掌握）现互不重叠，计数之和等于当前单词本总词数；`项目说明.md` 已同步补充该口径说明。

### P2 · 「模糊」被计为回答正确

```js
const correct = rating !== "hard";   // medium(模糊) 也算正确
```

「今日正确率」会把「模糊」计入分子，正确率偏高、不能真实反映掌握程度。

**建议**：把正确率口径改为仅 `easy`/`mastered` 计为正确，或在统计里单独展示「模糊」占比。

---

## 四、轻微 / 建议

- **P3 · 各队列长度上限不一致**（app.js:1711）：
  `unmastered` / `mastered` 不截断（可能上千条横穿），而 `all` / `wrong` / `new` 硬截 40。建议统一策略，超长队列改为分页/分批或明确上限。
- **P3 · 「今日待复习」把复现副本也算进去**（app.js:2307）—— ✅ 已修复（v95）：
  原 `due = queueIds.length - index` 会把复现副本一并计入，与「还有多少不同的词」不一致。现改为 `getUniqueWordIds(queueIds.slice(index)).length`，只统计剩余的不同单词。
- **P3 · `applyExamSprintSchedule` 每次 `ensureBooks` 全量扫描**（app.js:1552，`ensureBooks` 被高频调用）：
  对每本书的每条 progress 都做一遍遍历/写回判断。词库上千后会有可感知开销，建议只在「首次迁移 / 载入」时跑一次，或用「已迁移」标记。
- **P3 · `getTodaySession()` 内含 `saveState()` 副作用**，且被 `renderStats` / `isPendingHard` 等高频调用，可能触发额外的 `localStorage` 写入与云同步排程。建议把「读」与「惰性修正写」拆开。
- **云同步合并的注意点**（`mergeCloudProgress`）：合并后 `level/interval` 取较新一侧，而 `correct/wrong` 取自事件计数最大值，理论上可能出现「等级来自 A、计数来自 B」的混合态；虽不至于出错，但建议加一条自检日志，便于排查跨设备异常。

---

## 五、已验证正常的逻辑

- 每日会话冻结 + `reconcileTodaySession` 在词库变动/复习量调整后能正确重建并夹紧 `index`，边界处理（`Math.min/max`）稳妥。
- `scheduleNext` 的间隔与等级映射无符号错误；`applyExamSprintSchedule` 的 14 天封顶确实生效。
- 云同步的 AES-GCM 加解密、同步码规整（`[A-F0-9]{32}`）、`base64url` 编解码、冲突重试（`revision_conflict` 最多重试 2 次）实现严谨。
- Service Worker 对 app 资源采用网络优先 + 回退缓存，`index.html` 的 `?v=92` 版本号与 `CACHE_NAME` 一致，缓存升级路径正确。

---

## 六、建议修复顺序

1. **P0** `makeId` 空 id（数据损坏级，且已实际复现）
2. **P1** 内联复习与主队列统一（两套规则迟早产生数据分歧）
3. **P1** 「忘了」重排阶梯 / 已掌握降级（直接影响复习体验的核心语义）
4. **P2** 错词口径、分布统计、正确率口径（一致性与观感）
5. **P3** 性能与展示细节
