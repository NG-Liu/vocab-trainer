# 背单词项目说明

## 1. 项目概览

这是一个可在电脑和手机浏览器中使用的本地优先背单词网页应用，当前通过 GitHub Pages 发布，并配合 Cloudflare Worker 和 Cloudflare KV 提供共建词本提交、跨设备进度同步等能力。

项目当前版本：`v103`

当前在线地址：[https://ng-liu.github.io/vocab-trainer/](https://ng-liu.github.io/vocab-trainer/)

文档更新时间：2026 年 9 月 19 日

项目的设计重点是：

- 复习过程尽量简单，当前主流程为英译中。
- 学习记录优先保存在当前设备，即使暂时没有网络也可以继续使用。
- 每日复习队列和退出位置自动保存。
- 可选多个内置单词本。
- 通过同步码把多个设备的学习进度合并到一起。
- 用户上传的共建词本文件只保存原始 TXT，由所有者审核后再人工整理。

## 2. 技术组成

项目是一个不依赖前端框架的静态网页应用，主要由 HTML、CSS 和原生 JavaScript 组成。

| 文件 | 作用 |
| --- | --- |
| `index.html` | 页面结构、导航、表单、复习卡片和数据管理弹窗 |
| `style.css` | 页面布局、响应式样式、按钮、词库行和移动端样式 |
| `app.js` | 单词本、复习队列、评分、词库、统计、本地备份和前端交互 |
| `cloud-sync.js` | 同步码处理、AES-GCM 加密解密和同步请求客户端 |
| `sw.js` | Service Worker 和离线缓存 |
| `manifest.webmanifest` | PWA 安装到手机桌面的配置 |
| `core-book-data.js` | 考研英语核心词汇数据 |
| `cet4-book-data.js` | 大学英语四级词汇数据 |
| `cet6-book-data.js` | 大学英语六级词汇数据 |
| `submission-config.js` | 共建提交和进度同步 Worker 地址 |
| `backend/cloudflare-worker.js` | Cloudflare Worker 后端 |
| `wrangler.toml` | Worker 和 Cloudflare KV 配置 |
| `SUBMISSIONS.md` | 共建提交后端部署说明 |
| `项目说明.md` | 当前项目的完整说明 |

## 3. 当前内置单词本

页面顶部的“单词本”下拉框用于切换内置单词本。当前包含：

1. 默认单词本
2. 考研英语核心词汇
3. 大学英语四级词汇
4. 大学英语六级词汇
5. 高数积分公式
6. 高数中值定理
7. 高数泰勒展开
8. 测试单词本

默认单词本包含最初提供的英语单词及项目中后续加入的英语词条，当前共 `1032` 条（v97 起由手写笔记本与课程词表整理后持续追加）。测试单词本包含 10 个用于验证功能的简单英语单词。

数学单词本以公式、定理或展开式作为词条，使用 KaTeX 渲染；英语单词本使用英文单词、中文释义和例句。

当前项目没有提供面向普通用户的“在线新建单词本”功能。用户可以在当前单词本中手动添加单词或批量导入，但共建页面上传的文件不会自动变成可选单词本，必须由所有者审核并人工整理后再加入项目代码。

## 4. 页面功能

### 4.1 复习页

复习页包含以下选择项：

- 单词本：选择当前要复习的内置单词本。
- 队列：选择今日复习、未掌握、已掌握、未学、全部混合或错词优先。
- 每日复习量：可选 `50`、`100`、`150`、`200`，默认是 `50`。
- 开始：按照当前队列和单词本开始复习。

英语单词卡片还支持：

- 单词出现时预加载当前单词和下一单词的发音。
- 点击扬声器按钮播放发音。
- 优先使用有道词典音频；音频加载或播放失败时，尝试使用浏览器自带的英文语音合成。
- 点击“显示答案”后显示中文释义。
- 显示答案后出现 `Cambridge 词典` 链接，在新页面打开 Cambridge 的搜索结果。

数学单词本不显示英语发音和 Cambridge 词典链接。

### 4.2 词库页

词库页支持：

- 搜索英文单词或中文释义。
- 按状态筛选：全部、未学、学习中、已掌握、错词。
- 按不熟悉程度排序。
- 每个词条单独点击“显示”或“隐藏”中文释义。
- 右下角悬浮按钮对当前已经加载的列表执行“全部显示”或“全部隐藏”。
- 词库分批加载，每批最多显示 120 个词，可点击“显示更多”。
- 点击词条中除按钮以外的区域，在该词条下方展开内嵌复习卡片。
- 同一时间只展开一个内嵌复习卡片。

手动添加或批量导入的词条支持中英文。系统会为每个词条生成独立且稳定的内部 ID，中文词条同样可以正常保存、复习和跨设备同步（纯符号等无法生成可读 ID 的词条会退化为短哈希 ID）。

词库行中的状态使用圆点表示：

- 灰色：未学
- 蓝色：学习中
- 红色：错词
- 绿色：已掌握
- 深蓝色：完全掌握

词库的排序规则如下：

1. 未学单词统一放在最后。
2. 其他单词先按记忆等级从低到高排列，等级低的更靠前。
3. 等级相同时，错误次数较多的靠前。
4. 再比较正确次数、总复习次数和到期时间。
5. 最后使用该单词本的原始顺序或字母顺序作为稳定排序。

在词库内嵌复习中，评分会立即更新该词的进度，但当前列表不会因为状态改变而马上重排。用户离开词库后再次进入，或触发新的词库渲染时，列表才按最新状态重新排序。

### 4.3 共建页

共建页接收以下信息：

- 词本名称
- 用户邮箱
- 一个 TXT 文件

浏览器端只检查文件扩展名和大小，不读取、不解析、不整理 TXT 内容。文件提交后由 Cloudflare Worker 保存到 GitHub 的待审核目录，所有者再人工查看和处理。

### 4.4 进度页

进度页显示：

- 记忆分布：未学、初识、熟悉、掌握、完全掌握。五个区间互不重叠，各区间数量之和等于当前单词本总词数：「掌握」指 `4 <= level < 5`，「完全掌握」指 `level >= 5`。
- 最近复习记录：词条、评分和时间。

顶部统计显示：

- 今日待复习数量：只统计当天队列里**剩余的不同单词数**（当天队列里的词减去 `history` 里今天已评过分的词），不把“忘了”产生的复现副本重复计入，也不看游标位置。
- 当前单词本总词数
- 已掌握词数
- 今日正确率

## 5. 完整复习逻辑

### 5.1 单词进度字段

每个单词都有一份进度记录：

```js
{
  level: 0,
  interval: 0,
  dueAt: 时间戳,
  seen: 0,
  correct: 0,
  wrong: 0,
  lastReviewed: null
}
```

字段含义：

- `level`：记忆等级，通常为 `0` 到 `5`，也允许出现半级。
- `interval`：下一次计划复习间隔，单位为天。
- `dueAt`：下一次到期时间。当前按本地时间当天零点加间隔天数计算。
- `seen`：实际被记录评分的次数。
- `correct`：正确类评分次数，包括“模糊”“记住”“完全掌握”。
- `wrong`：真实记录的“忘了”次数。
- `lastReviewed`：最近一次真实记录评分的时间。

### 5.2 今日复习的选词原则

今日复习只从当前单词本中选择 `dueAt <= 当前时间` 的单词，不会为了凑足 50、100、150 或 200 个而强行加入尚未到期的单词。

候选单词排序为：

1. 到期时间更早的靠前。
2. 错误次数更多的靠前。
3. 使用单词本原始顺序或字母顺序稳定排序。

然后按照用户选择的每日复习量截取队列。因此，如果当前只有 37 个单词到期，即使选择了 200 个，今日队列也可能只有 37 个基础单词，页面不会补入未到期单词。

今日队列会保存到当前单词本的 `todaySession` 中，主要字段如下：

```js
{
  date: "YYYY-MM-DD",
  limit: 50,
  queueIds: [],
  index: 0,
  pendingHardId: null,
  hardReviewCounts: {},
  completedWordIds: [],
  updatedAt: 时间戳
}
```

其中 `queueIds` 会包含“忘了”产生的复现副本（同一个 id 出现多次），`hardReviewCounts` 记录每个词当天已安排的出现次数（忘过并排满复现的词为 4）。

如果用户复习到一半退出，当前索引会保存。下次打开并点击“今日复习”时，从上次的位置继续。当天完成队列后，继续点击“开始”不会重新生成一套新的今日队列；可以改选其他队列继续复习。

切换单词本时，每个单词本拥有独立的进度、历史和今日复习会话。

### 5.3 评分规则

#### 忘了

- 第一次真实点击“忘了”会记录一次历史。
- `wrong` 加 1，`seen` 加 1，`correct` 不变。
- 当前等级下降 1 级，最低为 0，最高只保留到 3，确保高等级单词会降到未掌握范围。
- 间隔设为 0 天，继续视为当天到期。
- 如果该词原来已经掌握，明确降为未掌握。
- 在今日复习中，答案会自动显示，并把按钮变成“下一个”。再次点击“下一个”才进入下一张卡片。
- 今日复习会把该词当天要复现的位置一次性排好：固定再复现 3 次（当天共出现 4 次），相对当前卡片约 4、14、34 张卡片后各出现一次。

#### 反复出现的忘了卡片

今日复习中因为“忘了”而重复出现的卡片，仍然使用原来的普通复习卡片，不使用特殊的新卡片界面。

- 点击一次“忘了”，当天就会固定安排 3 次复现，合计出现 4 次，间隔约 4、14、34 张卡片；复现时无论点什么，剩余的复现次数都照常出现。
- 复现卡片答案默认隐藏，仍然显示“显示答案”和评分按钮。
- 在复现卡片上点击任意评分按钮都只进入下一张：不新增历史记录，也不修改进度、正确率或错误次数。
- 也就是说，当天的学习进度只按第一次“忘了”记录，复现只用于再看一遍、加深印象；该词因为 `wrong` 已加 1、间隔归 0，第二天仍会优先到期。
- 参数由 `HARD_RETRY_GAPS` 控制（当前 `[4, 10, 20]`，累计偏移为 `[4, 14, 34]`）；当天每个词最多出现 `MAX_DAILY_HARD_REVIEWS` 次（当前 4 次）。
- 单次队列上限为“每日复习量 × 4”（例如选 50 时为 200 张，选 200 时为 800 张）。最坏情况（当天选中的单词全部忘光）刚好等于该上限，因此不会因为超上限而丢掉某次复现。
- 如果忘记的位置太靠后，复现位置会顺延到队列末尾，此时最后几次可能出现得比较密集。

#### 队列游标与「第 N 次复习」

- 今日复习的进度以 `history` 里当天的记录为准，队列里的 `index` 只是游标，**不代表已复习了多少个词**（复现卡只翻页、不写记录，卡片也可能被直接翻过去）。
- 因此卡片头与顶部统计的「已完成」数一律取「当天队列里、今天已评过分的不同单词数」，不按 `index` 折算。
- 「第 N 次复习」只对**真正的复现卡**显示（`hardReviewCounts[word.id]` 记着该词当天应出现的次数，默认 1）。同一个词在队列里出现两次并不等于复习了两遍，所以出现次数要按这个上限截断后再判断。
- 游标走到队尾、但当天队列里还有没复习过的词时，会把剩余的词**重新排成队列**并把游标归零（`extendExhaustedTodayQueue`），而不是把它们的 id 追加到队尾——追加会让同一个词在队列里出现两次，既虚增队列长度，又会被显示成「第 2 次复习」。
- **队列不变式**：同一个词在 `queueIds` 里出现的次数不得超过 `max(1, hardReviewCounts[word.id])`。
  `reconcileTodaySession()` 每次读取会话时都会清掉超额的副本，并把游标按「保留下来的卡」重新计数。
  这是一条自愈规则：即使会话被旧版本写坏（曾出现 389 张卡对应 200 个词），打开页面就会自动收敛回 230 张，不需要用户手动重建。

#### seeding 缓存

`ensureBooks()` 里的 seeding（灌内置词表、修正词条文案、按考纲压缩间隔）很贵：8 本词本上万个词条，
而 `ensureCurrentBook()` 会被 `getProgress()` / `getTodaySession()` 高频调用——实测每次约 7 ms，点一张卡要跑七八次。

现在用 `booksSignature()`（各词本的「词数 : 排序模式」）当签名，签名不变就跳过 seeding，实测降到 0.01 ms，
`renderAll()` 从 44 ms 降到 3 ms。**代价是：凡是整体替换或大改词本的地方，必须把 `ensuredBooksSignature` 置空**
（`replaceState()` 和 `applyCloudSyncSnapshot()` 里已经这么做了），否则新的内置词不会被补进来。

#### 词条改名后的进度迁移（旧 id 别名表）

词条 id 由 `makeId(term)` **每次现算、不落盘**，所以**改 term 就等于换 id**。
历史上有一批词条因 OCR 拆词导致 term 损坏（如 `rmost`、`outeintegral`、`overse as`），
修正 term 后 id 会从 `rmost` 变成 `utmost`，用户已存的 `progress` / `history` 会全部指向旧 id 而**失去进度**。

解决办法是「旧 id → 新 id」别名表，挂在数据文件末尾：

```js
// cet6-book-data.js 末尾
window.LEGACY_ID_ALIAS = Object.assign(window.LEGACY_ID_ALIAS || {}, {
  "rmost": "utmost",
  "outeintegral": "integral"
});
```

`app.js` 的 `inheritLegacyProgress(book, newId)` 在 `seedBookWords()` 里被调用两次
（新词条分支 + 已有词条分支），做三件事：

1. **继承进度**：把旧 id 的 `progress` 搬到新 id 下；**仅当新 id 还没有真实进度时**
   （`!book.progress[newId] || seen === 0`）才搬，避免覆盖用户新产生的记录。
2. **删除旧键**：`delete book.progress[oldId]`，防止同一份学习记录被算两次。
3. **改写历史**：把 `history[].wordId === oldId` 的项改成 `newId`。

**用户在旧设备上零操作**——下次打开页面时 seeding 自动完成迁移。别名表是全局的，
同一 id 在多本词本（如 `stern` 同时存在于 `default`/`kaoyan-core`/`cet4`/`cet6`）里都会被处理。

> 维护约定：**以后任何改 term 的批量修复，都要同步往对应数据文件的 `LEGACY_ID_ALIAS` 里追加映射**，
> 否则用户进度会静默丢失。验证方法见 `.workbuddy/_verify_migration.js`（注入真实备份比对迁移前后数值）。

#### 模糊

- 记录一次真实评分。
- `seen` 加 1，`correct` 加 1。
- 记忆等级增加半级，最高为 5。
- 固定安排在 1 天后复习。

#### 记住

- 记录一次真实评分。
- `seen` 加 1，`correct` 加 1。
- 记忆等级增加 1 级，最高为 5。
- 初次复习通常安排 1 天后复习。
- 已经有复习间隔时，新的间隔约为原间隔乘以 `2.6`，至少为 2 天。
- 最大复习间隔为 14 天。

#### 完全掌握

- 记录一次真实评分。
- 记忆等级直接设为 5。
- 间隔至少为 7 天，并且不超过 14 天。

当前项目是考试冲刺配置，最大间隔被限制为 14 天，而不是长期记忆系统常见的更长间隔。

### 5.4 非今日复习队列

复习页还提供以下队列：

- 未掌握单词：`level < 4`，不限制为 40 个。
- 已掌握单词：`level >= 4`，不限制为 40 个。
- 未学单词：`seen === 0`，最多取 40 个。
- 错词优先：`wrong > 0`，最多取 40 个。
- 全部混合：当前单词本全部单词，最多取 40 个。

非今日队列通常按照到期时间、错误次数和单词本顺序排序。非今日队列中的每次评分都会正常记录并更新进度，不使用今日复习的重复卡片免记录规则。

### 5.5 内嵌单词复习

从词库点击词条后，不会跳到复习页，而是在该词条下方展开一张内嵌复习卡片。

内嵌卡片：

- 答案默认隐藏。
- 支持显示答案、Cambridge 词典链接、发音和四种评分。
- 点击“模糊”“记住”或“完全掌握”后更新进度并自动收起。
- 第一次点击“忘了”会记录进度、显示答案并把按钮变成“下一个”。
- 再次点击“下一个”后收起，不重复记录。
- 评分后当前词库列表保持原有显示顺序，不立即重排。
- 离开词库再进入时，才按最新进度重新排序。

## 6. 词库筛选和排序细节

搜索会同时匹配单词和中文释义，即使该词的释义当前处于隐藏状态，也仍然可以被搜索到。

状态筛选含义：

- 未学：`seen === 0`
- 学习中：`seen > 0 && level < 4`
- 已掌握：`level >= 4`
- 错词：`wrong > 0`，但状态点的显示优先级会把低等级错词显示为红点

词库的展开释义状态、当前内嵌复习词条和当前冻结排序只保存在页面运行期间，不写入 `localStorage`。刷新页面后，每个词的释义默认隐藏，列表重新按照最新进度排序。

## 7. 本地数据保存

主要本地存储键：

```text
wordTrainer.v1
```

本地数据包括：

- 所有内置单词本和本地添加的额外词条。
- 每个单词的进度。
- 每个单词本的复习历史，最多保留最近 500 条。
- 当天复习队列、当前索引和重点复习信息。
- 当前选择的单词本。
- 当前每日复习量。

页面还提供“数据”弹窗：

- 导出备份：下载完整 JSON 备份，文件名类似 `vocab-backup-2026-09-10.json`。
- 导入备份：选择 JSON 文件并确认后，用备份覆盖当前设备的本地数据。
- 生成同步码、连接同步码、立即同步和断开同步。

导入备份会覆盖当前本地数据，使用前应确认备份内容正确。

### 7.1 载入时的数据净化（重要）

`normalizeState()` → `normalizeBookState()` 会在**读 localStorage 和导入备份时**把每本书收敛成安全形状：
`words` 里缺 `term` 的条目、`history` 里缺 `id`/`at` 的条目直接丢弃，`progress` 里非数字的字段回落到默认值，
`terms` 相同 id 只保留第一份，`history` 按 `eventId` 去重并按时间倒序截到 500 条。

原因：这些数据会先写进 `localStorage`，一旦渲染抛错，**下次打开页面会在 `init()` 里直接白屏**，
用户不清站点数据就再也进不来。任何后续改动都不要绕过这一层。

### 7.2 存储容量

单键体积实测约 **2.4 MB**（8 本词本全部 seeding 之后），浏览器 `localStorage` 上限通常 5 MB。
`saveState()` 已经用 `try/catch` 包住写入：配额写满时不会把复习流程带崩，而是弹一次提示，
告诉用户先导出备份再删词。**加词本/大批加词前先看一眼剩余空间。**

## 8. 跨设备同步

### 8.1 同步码

同步码由浏览器生成，底层为 128 位随机数据，显示为四组十六进制字符，例如：

```text
XXXXXXXX-XXXXXXXX-XXXXXXXX-XXXXXXXX
```

同步码同时用于：

- 推导云端记录 ID。
- 推导请求鉴权摘要。
- 推导 AES-GCM 加密密钥。

同步码本身不会写入 JSON 备份。遗失同步码后，无法解密云端进度。

### 8.2 同步内容

同步内容包括每个单词本的：

- 本地额外添加的单词。
- 有意义的进度记录。
- 最近复习历史。
- 今日复习会话。

内置词条本身已经随网页代码发布，因此云端主要同步用户的进度、历史和额外词条。

### 8.3 加密和存储

浏览器先使用 AES-GCM 加密整个进度快照，再把密文发送给 Cloudflare Worker。Worker 只把密文、版本号、修订号和更新时间存进 Cloudflare KV，不直接读取用户的单词进度内容。

GitHub 仓库不保存跨设备进度。GitHub 只用于静态网页文件和共建上传文件。

### 8.4 自动同步和冲突处理

- 本地数据变化后，在线状态下约 1.8 秒后自动同步。
- 页面处于可见状态时，约每 60 秒检查一次云端变化。
- 页面恢复联网或重新变为可见时，会尝试同步。
- 推送使用修订号检查并发修改。
- 出现修订冲突时，客户端重新拉取并尝试合并，最多自动重试一次。
- 复习历史按事件 ID 去重，避免同一事件重复计数。
- 两台设备都离线时，各自先保存本地；之后联网同步时，会进行尽可能的历史、进度和今日会话合并。
- **今日会话的游标不跨设备比较**（v106）。两台设备各排各的队列，游标位置根本不可比：
  合并时不再取 `max(local.index, remote.index)`——曾经这样取，一台设备刚点完「重建今日队列」
  （队列全新、游标 0），1.8 秒后同步就把另一台设备的游标整段搬过来、直接落到队尾，
  表现为「明明还没背，卡片头却跳到尽头、剩余量乱跳」。
  现在按合并后的复习记录把游标定位到**第一张今天还没评过的卡**，两台设备算出的结果一致。
  读取会话时还有一条自愈规则：游标只可能**往回**退到第一张未复习的卡，绝不会往前跳。

同步不是账号系统，也不是实时协作编辑。同步码应视为访问凭据，不要公开发给其他人。

## 9. 共建词本提交流程

### 9.1 前端提交

用户在共建页填写词本名称、邮箱并选择 TXT 文件。前端限制：

- 只接受 `.txt` 文件。
- 文件大小不能超过 1 MB。
- 不在本地读取或解析原始文本内容。

### 9.2 Worker 处理

前端向当前 Worker 地址发送 `multipart/form-data` POST 请求：

```text
POST /
```

字段为：

```text
bookName
email
file
```

Worker 会再次检查词本名称、邮箱、文件类型和文件大小，然后通过 GitHub Contents API 写入：

```text
submissions/pending/YYYY-MM-DD-xxxxxxxx/source.txt
submissions/pending/YYYY-MM-DD-xxxxxxxx/metadata.json
```

`source.txt` 是用户上传的原始文件，`metadata.json` 记录提交编号、词本名称、邮箱、原始文件名、文件大小、提交时间和审核状态。

### 9.3 审核

所有者在 GitHub 仓库的 `submissions/pending/` 中查看文件，审核原始 TXT 内容后，再决定：

- 是否接受。
- 如何拆分单词、释义和例句。
- 放入哪个正式单词本。
- 是否需要去重或修正内容。

提交不会自动创建单词本，也不会自动把原始 TXT 内容加入网页。

## 10. 后端部署

当前 Worker 配置文件为 `wrangler.toml`，主要环境变量为：

```text
GITHUB_OWNER=NG-Liu
GITHUB_REPO=vocab-trainer
GITHUB_BRANCH=main
ALLOWED_ORIGIN=https://ng-liu.github.io
```

Worker 还绑定了名为 `PROGRESS_SYNC` 的 Cloudflare KV namespace，用于保存加密同步记录。

部署基本步骤：

1. 安装并登录 Wrangler。
2. 在 GitHub 创建只允许目标仓库 Contents 读写权限的 fine-grained token。
3. 使用 Wrangler Secret 保存 `GITHUB_TOKEN`，不要把 token 写入前端或提交到仓库。
4. 确认 `wrangler.toml` 中的 KV namespace ID 正确。
5. 在项目目录执行 Worker 部署命令。
6. 将 Worker 在线地址写入 `submission-config.js`。
7. 提交并推送前端文件到 GitHub Pages 使用的分支。

当前前端配置的 Worker 地址为：

```text
https://vocab-trainer-submission.liunianguo671.workers.dev
```

详细后端说明见 [`SUBMISSIONS.md`](./SUBMISSIONS.md)。

## 11. GitHub Pages 和手机访问

GitHub Pages 负责提供静态网页，不负责接收上传文件。用户上传文件必须通过 Cloudflare Worker 转发到 GitHub。

项目包含 PWA 配置和 Service Worker：

- 页面可以在支持的浏览器中安装到手机桌面。
- Service Worker 会缓存主要网页资源，使基础界面在无网络时仍可打开。
- 发音、Cambridge 词典和云同步仍依赖网络。
- 当前 Service Worker 缓存名为 `word-trainer-v103`。
- 前端资源 URL 和缓存版本均使用 `v103`。
- 更新前端代码时，应同步修改 `app.js` 中的 `APP_VERSION`、`index.html` 的资源版本和 `sw.js` 的缓存版本。

页面右上角的“桌面图标”按钮会调用浏览器安装能力。若浏览器没有提供安装事件，会显示对应浏览器的手动安装提示。

## 12. 安全和隐私注意事项

- GitHub Pages 上的前端代码和内置单词数据是公开的。
- GitHub token 只能保存在 Cloudflare Worker Secret 中，不能放入 `app.js`、`submission-config.js` 或网页源码。
- 共建提交的邮箱会写入 GitHub 的 `metadata.json`，提交者应确认接受这一点。
- 同步进度在浏览器端加密，但同步码丢失后无法恢复云端数据。
- 掌握同步码的人可以访问对应的加密进度记录，因此不要把同步码发布到公共位置。
- 共建接口当前主要依靠文件类型、大小、邮箱格式和 CORS 限制，没有登录账户和人工身份认证系统。

## 13. 当前已知限制

1. 今日复习不会使用未到期单词补足选择的数量。
2. “未学”“错词优先”“全部混合”队列当前最多取 40 个；“未掌握”和“已掌握”不限制为 40 个。
3. 每日复习队列按本地日期和本地时区生成。
4. 没有用户登录系统，跨设备识别依靠同步码。
5. 同步是加密快照合并，不是逐操作的实时数据库；复杂的同时修改仍应通过导出备份保留副本。
6. 发音服务依赖外部音频地址，网络、地区或服务状态可能影响成功率；失败时才尝试浏览器语音合成。
7. Cambridge 词典链接会打开外部网站，具体词条是否匹配由 Cambridge 搜索结果决定。
8. 词库“全部显示/隐藏”作用于当前筛选结果中已经加载到页面的词条；更多词条加载后，按钮会根据新列表重新判断。
9. 共建上传只保存原始 TXT，不会自动解析、去重、翻译或创建正式单词本。
10. 用户在当前词库中手动添加的词条保存在本地；只有连接同步码后，额外词条才会参与跨设备同步。
11. 词库页的「内嵌复习」与复习页是两条评分路径：内嵌复习的「忘了」同样会记为错词、间隔归零、当天到期，
    但**不会**像复习页那样额外安排 3 次当天复现（`markTodaySessionPendingHard` 依赖复习游标，内嵌复习没有游标）。
12. 云同步的合并方向按 `lastReviewed` 时间戳判定：两台设备系统时间偏差过大时，理论上有进度被回退的可能。
13. 云端和本地都只保留最近 500 条复习记录；当天评过 500 次以上时，「今日正确率」只统计最近 500 条。
14. 单键本地存储约 2.4 MB，接近浏览器 5 MB 上限；写满时 `saveState()` 会提示但不再落盘。

## 14. 常用维护检查

### 14.1 回归测试

本地有一套可直接跑的不变量测试（`.workbuddy/_harness.js` + `.workbuddy/_tests.js`，**该目录不进仓库**）：
用 DOM 桩把 `app.js` 原封不动跑在 Node 里，注入真实备份，覆盖复习、跨天、队尾重排、畸形备份导入、
云同步合并幂等性、词库内联复习、200 步随机操作模糊测试，以及性能微基准。共 143 条断言。

```powershell
cd C:\Users\mechrevo\Desktop\背单词
node .workbuddy/_tests.js
```

**改动复习队列 / 会话状态 / 导入导出之后，先跑一遍再发布**（换机器时需先重建这两个文件）。

改过 term（或批量修词条）之后，另外跑一次迁移验证，确认旧进度没丢：

```powershell
node .workbuddy/_verify_migration.js
```

它会注入真实备份，逐字段比对「迁移后的新 id 进度」与「迁移前的旧 id 进度」，
并检查旧键已删除、`history` 无残留旧 id、词条数不变。

### 14.2 常规检查

修改项目后建议执行：

```powershell
node --check app.js
node --check cloud-sync.js
git diff --check
```

如果修改了前端资源，必须同时检查：

- `app.js` 中的 `APP_VERSION`。
- `index.html` 中所有资源的 `?v=` 参数。
- `sw.js` 中的缓存名和资源版本。
- 是否需要清理旧的 Service Worker 缓存。

当前仓库的主要发布分支为 `main`，远程仓库为：

```text
https://github.com/NG-Liu/vocab-trainer.git
```

## 15. 一次完整使用流程

1. 打开在线地址或手机桌面应用。
2. 选择要学习的单词本。
3. 在复习页选择队列和每日复习量。
4. 点击“开始”，看到英文后回忆中文释义。
5. 需要时点击发音或“显示答案”。
6. 根据记忆情况选择“忘了”“模糊”“记住”或“完全掌握”。
7. 中途退出时无需额外保存，今日复习位置会自动保存。
8. 在另一台设备打开页面后输入同一个同步码，即可拉取并合并进度。
9. 在词库页搜索、筛选、展开释义或点击词条进行单词复习。
10. 在共建页提交 TXT 后，到 GitHub 的 `submissions/pending/` 中审核原始文件。

